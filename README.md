# ইন্টার্ভিউ প্রশ্ন - ব্লগ টাস্ক
## 🔹 Interfaces বনাম Types এর পার্থক্য
 TypeScript-এ `interface` এবং `type` উভয়ই অবজেক্টের আকৃতি বর্ণনা করতে ব্যবহার করা হয়, তাদের মধ্যে কিছু পার্থক্য রয়েছেঃ
- `interface` #
  ১। extends কীওয়ার্ড ব্যবহার করে ইনহেরিট করে, Interface একাধিকবার ঘোষণা করলে সেগুলো merge হয়।
  ২। অবজেক্ট আকৃতি এবং ক্লাস ইমপ্লিমেন্টেশনের জন্য ব্যবহার করা হয়। 
  ৩। কম্পাইল হয় দ্রুত।
- `type` #
  ১। & অপারেটর ব্যবহার করে ইনহেরিট করা যায়, Type একবারই ঘোষণা করা যায়, পুনরায় ঘোষণা করলে error দেয়। 
  ২। ইউনিয়ন, ইন্টারসেকশন, প্রিমিটিভ টাইপের জন্য ব্যবহার করা হয়।
  ৩। কম্পাইল কিছুটা ধীর। 
``` Typescript
// Interface - ডিক্লারেশন মার্জিং
interface User {
  name: string;
}

interface User {
  age: number;
}
// এখন User ইন্টারফেসে name এবং age উভয়ই আছে

// Type - ইন্টারসেকশন
type Animal = {
  species: string;
};

type Bear = Animal & {
  honey: boolean;
};
```
## 🔹 keyof কীওয়ার্ডের ব্যবহার
 `keyof` টাইপ অপারেটর একটি অবজেক্ট টাইপ গ্রহণ করে এবং এর সব `key` গুলিকে একটি স্ট্রিং লিটারেল ইউনিয়ন টাইপ হিসেবে রিটার্ন করে। টাইপ-সেফ প্রোপার্টি অ্যাক্সেসের জন্য খুবই কাজের এটি।
 ```typescript
interface Person {
  name: string;
  age: number;
}

type PersonKeys = keyof Person; // "name" | "age"

function getProperty(obj: Person, key: keyof Person) {
  return obj[key];
}

const person: Person = { name: "Alice", age: 30 };
const name = getProperty(person, "name"); // টাইপ-সেফ
```
### ব্যবহার
- জেনেরিক ফাংশনে টাইপ-সেফটি নিশ্চিত করার জন্য। 
- ম্যাপড টাইপ তৈরি করতে ব্যবহার করা হয়। 
- অবজেক্টের প্রোপার্টি ডাইনামিকভাবে অ্যাক্সেস করতে ব্যবহার করা হয়।

- 
